<local:WszystkieViewBase x:Class="BikeRental.Views.PlanCenowyStawkaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BikeRental.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding StawkaId}"/>
                <DataGridTextColumn Header="Nazwa" Binding="{Binding PlanCenowyNazwa}"/>
                <DataGridTextColumn Header="Od minuty" Binding="{Binding OdMinuty}"/>
                <DataGridTextColumn Header="Do minuty" Binding="{Binding DoMinuty}"/>
                <DataGridTextColumn Header="Cena za min" Binding="{Binding CenaZaMin}"/>
                <DataGridTextColumn Header="Oplata startowa" Binding="{Binding OplataStartowa}"/>
                <DataGridTextColumn Header="Darmowe minuty" Binding="{Binding LimitDarmowychMin}"/>
                <DataGridTextColumn Header="Doplata" Binding="{Binding DoplataPoLimicie, StringFormat={}{0:F2}}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
