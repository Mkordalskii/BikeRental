<UserControl x:Class="BikeRental.Views.SymulacjaWypozyczeniaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BikeRental.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White" Width="450" HorizontalAlignment="Left">
        <Grid.RowDefinitions>
            <RowDefinition Height="33"/>
            <RowDefinition Height="33"/>
            <RowDefinition Height="33"/>
            <RowDefinition Height="33"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="33"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0"
               Content="Symulacja kosztu wypożyczenia (godziny / dni)"
               HorizontalContentAlignment="Center"/>

        <!-- Plan cenowy -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Content="Plan cenowy:" Grid.Column="0" Margin="3"/>
            <ComboBox Grid.Column="1" Margin="3"
                      ItemsSource="{Binding PlanyCenoweComboBoxItems}"
                      DisplayMemberPath="Value"
                      SelectedValuePath="Key"
                      SelectedValue="{Binding PlanCenowyId, Mode=TwoWay}" />
        </Grid>

        <!-- Godziny -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Content="Godziny:" Grid.Column="0" Margin="3"/>
            <TextBox Grid.Column="1" Margin="3"
                     Text="{Binding Godziny, UpdateSourceTrigger=PropertyChanged}" />
        </Grid>

        <!-- Dni -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Content="Dni:" Grid.Column="0" Margin="3"/>
            <TextBox Grid.Column="1" Margin="3"
                     Text="{Binding Dni, UpdateSourceTrigger=PropertyChanged}" />
        </Grid>

        <!-- Przyciski -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Oblicz" Margin="5" Width="120" Command="{Binding ObliczCommand}" />
            <Button Content="Wyczyść" Margin="5" Width="120" Command="{Binding WyczyscCommand}" />
        </StackPanel>

        <!-- Wynik -->
        <Grid Grid.Row="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Content="Koszt wynosi:" Grid.Column="0" Margin="3"/>
            <Label Grid.Column="1" Margin="3" HorizontalAlignment="Left" Content="{Binding Koszt}" />
        </Grid>
    </Grid>
</UserControl>
