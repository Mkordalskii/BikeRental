<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:BikeRental.Views">


    <Style TargetType="{x:Type local:WszystkieViewBase}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:WszystkieViewBase}">
                    <Grid Margin="0,10,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="45"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <ToolBar Grid.Row="0" Height="45" Margin="0,2,0,2">
                            <ToggleButton Height="40" Width="40" ToolTip="Dodaj" Margin="0,0,5,0" Command="{Binding AddCommand}">
                                <Image Source="/Resources/Icons/Dodaj.png" Width="35" Height="35" RenderOptions.BitmapScalingMode="HighQuality"/>
                            </ToggleButton>

                            <ToggleButton Height="40" Width="40" ToolTip="Edytuj" Margin="0,0,5,0">
                                <Image Source="/Resources/Icons/Edytuj.png" Width="35" Height="35" RenderOptions.BitmapScalingMode="HighQuality"/>
                            </ToggleButton>

                            <ToggleButton Height="40" Width="40" ToolTip="Usun" Margin="0,0,5,0">
                                <Image Source="/Resources/Icons/Usun.png" Width="35" Height="35" RenderOptions.BitmapScalingMode="HighQuality"/>
                            </ToggleButton>

                            <ToggleButton Height="40" Width="40" ToolTip="Odswiez" Command="{Binding LoadCommand}">
                                <Image Source="/Resources/Icons/Odswiez.png" Width="35" Height="35" RenderOptions.BitmapScalingMode="HighQuality"/>
                            </ToggleButton>
                        </ToolBar>
                        <Expander Grid.Row="1" Background="Azure" Header="Sortowanie i filtrowanie">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition  Height="25"/>
                                    <RowDefinition  Height="25"/>
                                    <RowDefinition  Height="30"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition  Width="230"/>
                                    <ColumnDefinition  Width="60"/>
                                    <ColumnDefinition  Width="230"/>
                                </Grid.ColumnDefinitions>
                                <Label  Grid.Row="0"  Grid.Column="0" Content="Sortowanie:  "/>
                                <ComboBox  Grid.Row="1"  Grid.Column="0" ItemsSource="{Binding SortComboBoxItems}"  SelectedValue="{Binding Path=SortField,Mode=TwoWay}"  Margin="0,0,60,0"/>
                                <Button  Content="Sortuj"  Grid.Row="1"  Grid.Column="0" Command="{Binding SortCommand}" Margin="170,0,0,0"/>
                                <Label  Grid.Row="0"  Grid.Column="2" Content="Wyszukiwanie:  "/>
                                <ComboBox  Grid.Row="1"  Grid.Column="2" ItemsSource="{Binding FindComboBoxItems}"  SelectedValue="{Binding Path=FindField,Mode=TwoWay}"  />
                                <TextBox  Grid.Row="2"  Grid.Column="2" Margin="0,5,60,0"  Text="{Binding FindTextBox}"/>
                                <Button  Content="Szukaj"  Grid.Row="3"  Grid.Column="2" Command="{Binding FindCommand}" Margin="170,5,0,0"/>
                            </Grid>
                        </Expander>
                        <ContentPresenter Grid.Row="2" Margin="0,5,0,5" /> <!--w tym miejscu wyswietlaj zawartosc widoku ktory bedzie dziedziczyl z tego widoku-->
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type local:JedenViewBase}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:JedenViewBase}">
                    <Grid Margin="0,10,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="45"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <ToolBar Grid.Row="0" Height="45" Margin="0,2,0,2">
                            <ToggleButton Height="40" Width="40" Margin="0,0,10,0" ToolTip="Zapisz i zamknij" Command="{Binding SaveAndCloseCommand}">
                                <Image Source="/Resources/Icons/Zapisz.png" Width="35" Height="35" RenderOptions.BitmapScalingMode="HighQuality"/>
                            </ToggleButton>

                            <ToggleButton Height="40" Width="40" ToolTip="Zapisz i zamknij">
                                <Image Source="/Resources/Icons/Anuluj.png" Width="35" Height="35" RenderOptions.BitmapScalingMode="HighQuality"/>
                            </ToggleButton>
                        </ToolBar>
                        <ContentPresenter Grid.Row="1" Margin="0,5,0,5"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
