<local:WszystkieViewBase x:Class="BikeRental.Views.RezerwacjeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BikeRental.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding RezerwacjaId}" />
                <DataGridTextColumn Header="Nazwa" Binding="{Binding KlientImie}"/>
                <DataGridTextColumn Header="Waluta" Binding="{Binding KlientNazwisko}"/>
                <DataGridTextColumn Header="Opis" Binding="{Binding KodFloty}"/>
                <DataGridTextColumn Header="AktywnyOd" Binding="{Binding DataOd}"/>
                <DataGridTextColumn Header="AktywnyDo" Binding="{Binding DataDo}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
