<local:WszystkieViewBase x:Class="BikeRental.Views.AbonamentyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BikeRental.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding AbonamentId}"/>
                <DataGridTextColumn Header="Imie klienta" Binding="{Binding ImieKlienta}"/>
                <DataGridTextColumn Header="Nazwisko klienta" Binding="{Binding NazwiskoKlienta}"/>
                <DataGridTextColumn Header="Plan Cenowy" Binding="{Binding PlanCenowyNazwa}"/>
                <DataGridTextColumn Header="Start" Binding="{Binding DataStart}"/>
                <DataGridTextColumn Header="Koniec" Binding="{Binding DataKoniec}"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
