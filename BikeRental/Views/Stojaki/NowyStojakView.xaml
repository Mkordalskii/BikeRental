<local:JedenViewBase x:Class="BikeRental.Views.NowyStojakView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BikeRental.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>


        </Grid.RowDefinitions>
        <Label Grid.Column="0" Grid.Row="0" FontSize="10" Content="Stacja" Margin="0,8,0,8"/>
        <ComboBox Grid.Column="1" Grid.Row="0"
          ItemsSource="{Binding StacjaItems}"
          DisplayMemberPath="Nazwa"
          SelectedValuePath="StacjaId"
          SelectedValue="{Binding StacjaId, Mode=TwoWay}"
          Margin="0,8,0,8"
          Width="200"/>

        <Label Grid.Column="0" Grid.Row="1" FontSize="10" Content="Numer miejsca" Margin="0,8,0,8"/>
        <TextBox Grid.Column="1" Grid.Row="1" Margin="0,8,0,8" Text="{Binding Path=NumerMiejsca, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Grid.Column="0" Grid.Row="2" FontSize="10" Content="Sprawny" Margin="0,8,0,8"/>
        <StackPanel Grid.Column="1" Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,8">
            <RadioButton Content="Tak"
                 GroupName="Aktywnosc"
                 IsChecked="{Binding Sprawny, Mode=TwoWay}" />
            <RadioButton Content="Nie"
                 GroupName="Aktywnosc"
                 IsChecked="{Binding Niesprawny, Mode=TwoWay}"
                 Margin="3,0,0,0"/>
        </StackPanel>
    </Grid>
</local:JedenViewBase>
